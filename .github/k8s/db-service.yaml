apiVersion: v1
kind: Service
metadata:
  namespace: nita
  name: db
  labels:
    io.kompose.service: db
spec:
  ports:
    - port: 3306
      protocol: TCP
      targetPort: 3306
  selector:
    io.kompose.service: db
  type: ClusterIP
